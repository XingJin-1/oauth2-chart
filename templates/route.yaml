kind: Route

apiVersion: route.openshift.io/v1

metadata:
  name: xing-appserver-route
  
spec:
  host: oauth2-playground-xj-1.eu-de-3.icp.infineon.com
  to:
    kind: Service
    name: xing-appserver-service
  tls:
    termination: reencrypt
  wildcardPolicy: None
  